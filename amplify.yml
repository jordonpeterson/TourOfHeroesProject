version: 1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    preBuild:
      commands: [cd tour-of-heroes]
    build:
      commands: [npm run-script build]
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: ./tour-of-heroes/dist
    files:
      - '**/*'
  cache:
    paths: []
